Start with the app but with no modal.
Create a `ModalScreen` with a single label and show it's transparent.
Add containers and show transparency goes away.
Restrict container size to show transparency again.
